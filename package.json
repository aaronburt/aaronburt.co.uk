{
	"name": "aaronburt",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"vite:build": "npm run build --prefix public",
		"deploy": "wrangler deploy",
		"dev": "npm run vite:build && wrangler dev",
		"start": "wrangler dev",
		"full:deploy": "npm run vite:build && npm run deploy"
	},
	"devDependencies": {
		"vite-plugin-sri": "^0.0.2",
		"wrangler": "^4.19.1"
	},
	"dependencies": {
		"@tailwindcss/vite": "^4.1.8",
		"tailwindcss": "^4.1.8"
	}
}
